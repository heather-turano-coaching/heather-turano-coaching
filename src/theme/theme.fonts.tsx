import { FontWeights } from "./theme.types";

export const fontWeightValues: { [key in FontWeights]: number } = {
  light: 200,
  regular: 400,
  semiBold: 600,
  bold: 700,
  extraBold: 800,
  black: 900
};
